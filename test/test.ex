
# This is a comment.

fn print(x: int) {
  
}

fn on_event(event: string, cb1: fn(), cb1: fn(string,): string) {

}

fn fib(n: int): int {
  if n <= 1 {
    return n;
  }
  let x;
  x = "10";
  let y;
  y ~= y;
  fib(y);
  return (fib(n - 1) + fib(n - 2));
}

fn main() {
  print(+fib(10));
}

fn test(n: int) {
  let x: int;
  test(x * 1);
}
