
# This is a comment.

fn print(x: int) {
  
}

fn fib(n: int): int {
  if n <= 1 {
    return n;
  }
  let x;
  x = "10";
  let y;
  fib(y);
  return (fib(n - 1) + fib(n - 2)) * "10";
}

fn main() {
  print(+fib(10));
}

fn test(n: int) {
  let x: string;
  test(x * 1);
}
